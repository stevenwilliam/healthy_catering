# Internationalisation — Evermore

**Languages:** Bahasa Indonesia (`id`, default), English (`en`), Simplified
Chinese (`zh`). Added 2026-08-18 on Steven's instruction; `id` and `en` were
already the plan (CLAUDE.md §10), `zh` is new.

---

## 1. Where the strings live

**No literal copy in a template or a component.** Every visible string is a key
in a catalogue, which is what makes a fourth language a data edit rather than a
rewrite.

| Surface | Catalogue | Lookup |
|---|---|---|
| Public pages (Go templates) | `internal/adapter/http/messages.go` | `{{t .L "key"}}` |
| App (React) | `web/src/lib/messages.ts` | `const t = useT()` → `t('key')` |
| Notifications | `internal/adapter/notify/templates.go` | per-locale map on each message |

The mechanism is `internal/platform/i18n`, which is business-agnostic and
portable: locales, negotiation, path prefixes and `Catalog` lookup, and nothing
about catering.

**Fallback is Indonesian, never blank.** A key missing a Chinese string renders
the Indonesian one. A key that does not exist at all renders *itself* — ugly on
purpose, so it surfaces in review rather than shipping as silence.

**The catalogue is enforced, on both sides.** `TestPublicMessagesComplete` and
`TestCopyBookCoversEveryLocale` fail the Go build if any key or any
notification lacks a locale. On the React side the `Entry` type is
`Record<Locale, string>` with every locale required, so a half-translated key
is a *compile* error. A missing string cannot land quietly on either surface.

## 2. How a locale is chosen

The two surfaces answer this differently, and deliberately.

**Public pages — the URL carries it.**

```
/                 /en/                /zh/
/menu/keto        /en/menu/keto       /zh/menu/keto
```

The default locale keeps the bare paths it has always had, so no existing link,
bookmark or indexed URL breaks. A cookie was rejected: it serves three
different bodies at one address, which breaks sharing a link and hands a search
engine one URL with three contents.

Each page therefore ships `hreflang` alternates for all three plus `x-default`,
and `sitemap.xml` lists every page in every language with `xhtml:link`
alternates on each entry.

**The app — a stored preference.** `/app` is `noindex` and behind a login, so
there is nothing to share or index and the opposite trade is right: the choice
lives in `localStorage` under `evermore.lang`, follows the customer between
screens, and changes no routes. First visit falls back to `navigator.languages`,
matched on the primary subtag so `zh-TW` and `en-GB` still land somewhere sensible.

## 3. The language selector

Same design on both surfaces:

- **Closed, it is the current language's flag alone** (Steven, 2026-08-18 —
  the name took too much of the bar). Still not a generic globe: a globe says
  nothing about which language you are currently in, and the flag at least
  answers that at a glance. The control keeps its accessible name through
  `aria-label`, so what a screen reader announces is unchanged — it is the
  sighted label that is gone, and the open menu spells every language out.
- **Every option is written in its own language** (`Bahasa Indonesia`,
  `English`, `中文`). Someone who cannot read Indonesian must still be able to
  find their way out of it, which a list written in Indonesian prevents.
- **Switching keeps the reader on the same page.** `/zh/menu/keto` → `English`
  lands on `/en/menu/keto`, not on the home page.
- The current entry carries `aria-current` (public) or `aria-checked` (app)
  **and** a tick — never colour alone (99 §8).

Two implementation points that are not arbitrary:

**The flags are inline SVG, never the flag emoji.** Windows ships no glyphs for
regional-indicator pairs, so 🇮🇩 🇬🇧 🇨🇳 render as the letter boxes "ID" "GB"
"CN" on the browser most of this audience uses — in the one control that must
not be ambiguous.

**The flags are `aria-hidden` decoration; the NAME is the label.** A flag is a
country, not a language: English is not the flag of one country, and Chinese is
read in several. The flag is there because Steven asked for it and because it
is a fast visual anchor; it is not what carries the meaning.

**Where it sits is load-bearing too.** Both mastheads are CSS grid with the
picker explicitly placed beside the wordmark, and only the nav allowed to take
a second row. As wrapping flexboxes they orphaned the picker onto a line of its
own the moment the row overflowed — which, with the 19px/700 nav the mid-green
bar's contrast floor requires, happened from about 1100px down, not just on
phones. One row at ≥80rem, two below.

The public selector is a `<details>` disclosure with no JavaScript at all — the
CSP has no `'unsafe-inline'` and the marketing pages ship no bundle, and a
disclosure widget is keyboard-operable and announced as expandable for free.

## 4. What is NOT translated

**Catalogue content**: diet-type names and descriptions, meal names, food item
names, and the per-diet SEO overrides. These are database rows with a single
`name`/`description` column, so they render in whatever language they were
entered in, whichever locale is selected. Raised as **docs/03 Q-24** with a
proposed JSONB `translations` column — but the real question there is whether
the food is translated at all, which is Steven's call and an ongoing cost.

**Server-side validation messages**: the API returns already-worded errors, so
a form error still appears in the API's language. Fixing it properly means
error *codes* through `platform/apierror` that the client renders from its own
catalogue. Worth doing; not done.

**Server-side validation messages** are the one visible gap inside otherwise
translated screens: a rejected form still shows the API's wording.

## 4a. Type: neither brand face covers Chinese

Erode is a Latin display face and the Inter variable file is Latin/Greek/
Cyrillic, so **no Han character is rendered by a brand font**. A Chinese
headline is not set in Erode and cannot be; it falls through the stack.

`web/public/fonts/fonts.css` therefore names a `--font-cjk` list before the
generic fallback — PingFang on Apple, Microsoft YaHei on Windows, Noto on Linux
and Android. Nothing is downloaded: a CJK webfont runs to megabytes and every
platform already ships a good one. Without that list the fallback is the
browser's last-resort serif, which sitting beside the Latin text on the same
page reads as a rendering fault rather than as a second script.

The practical consequence: **the Chinese pages do not carry the brand's display
voice**, only its colour, layout and wordmark. Buying a CJK display face that
pairs with Erode is a brand decision and a licence cost, not an engineering
one.

## 6. Editable copy, and machine translation (Steven, 2026-08-18)

The home hero — eyebrow, headline, lede and button — is no longer compiled into
the binary. It lives in `public_content` and is edited in the back office at
**/app/admin/content**, behind `settings.write`.

The picture beside it is separate: it is the `public.hero_image` row in
`sys_parameters`, editable on the normal settings screen, so swapping the
photograph never needs a deploy.

### 6.1 The model

| | |
|---|---|
| **Indonesian** | the SOURCE. The only language anyone has to write. |
| **English, Chinese** | DERIVED from it by the translator. |
| **Override** | either may be written by hand, and is then permanent — the translator never touches it again. |
| **Stale** | an override whose Indonesian source has changed since. Flagged, never silently refreshed. |

The database enforces the parts that matter rather than trusting the
application: `locale` is constrained to the three, and a CHECK stops the source
row from claiming to be an override of itself.

`source_hash` is the sha256 of the Indonesian text a translation was made from.
Staleness is that hash no longer matching — which is what lets the screen say
"the Indonesian moved and your English did not" instead of quietly serving copy
that no longer matches.

### 6.2 Two guarantees worth stating

**A translator failure never fails a save.** The Indonesian is what a person
typed; losing it because a third party timed out would be far worse than
showing an untranslated string. The save commits, and the response reports per
language what happened — `translated`, `kept-override`, `no-translator` or
`failed` — so the editor sees the truth instead of a bare "saved".

**The page always renders.** The template reads `public_content` first and the
compiled catalogue second. A row that is missing, empty, untranslated, or a
database that is briefly unreachable, all fall through to the catalogue. No
edit can leave a heading blank.

### 6.3 The provider

Off by default, and off is a working state — the back office simply asks a
human for the English and Chinese.

```bash
TRANSLATE_PROVIDER=google     # or empty for off
TRANSLATE_API_KEY=...         # env only; it is a billable credential
```

`google` is Cloud Translation **v2**, which takes a plain API key; v3 wants a
service account and OAuth, which is a lot of setup for four strings on a
marketing page. `zh` is sent as `zh-CN`, matching what §2 says `zh` means here.

Adding DeepL or an LLM is a new `Translator` in
`internal/adapter/translate` — the service depends on the interface, not on a
vendor.

**Not yet configured on any environment**, so today every translation is
hand-written and the screen says so.

## 5. Status

| Surface | State |
|---|---|
| Public pages: home, menu, 404, chrome, selector | ✅ all three languages |
| SEO: hreflang, x-default, sitemap alternates, per-locale `<title>`/description | ✅ |
| Notification templates | ✅ all three languages |
| App shell: nav, sign in/out, footer, 404, selector | ✅ all three languages |
| App screens: login, register, menu, orders, order detail, packages, addresses, security | ✅ all three languages |
| Admin screens: payments, deliveries, settings | ✅ all three languages |
| Home hero copy: editable in the back office, 3 languages, override + stale | ✅ |
| Machine translation provider | ⬜ built and wired; no key configured, so every translation is manual |
| Catalogue content (menu items) | ⬜ single-language by schema — Q-24 |
| API validation messages | ⬜ rendered in the API's wording |

Roughly 300 keys across the two catalogues. What is left is not UI work:
the menu CONTENT is database rows (Q-24), and the API's error strings need
error *codes* through `platform/apierror` before a client can render them from
its own catalogue.
